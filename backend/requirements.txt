# Jren Campus Assistant 后端依赖
# Python >= 3.11
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3

# MCP 数据接入：iCal 解析（Windows 下 zoneinfo 需要 tzdata）
icalendar>=5.0
tzdata>=2024.1

# MCP Server 暴露层（QClaw 接入，Streamable HTTP）
mcp>=2.0.0
apscheduler>=3.10

# 测试
pytest>=8.0
httpx>=0.27
